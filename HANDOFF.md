# 오제하우스 랜딩페이지 — 개발 인수인계 가이드

퍼블리싱까지 완료된 정적 랜딩페이지입니다.
빌드 도구·프레임워크·패키지 의존성이 **전혀 없습니다.** `index.html`을 브라우저로 열면 그대로 동작합니다.

- 최종 확인 기준: Chrome / Safari 최신, 데스크톱 · 태블릿(1000px) · 모바일(390px)
- 작성 언어: HTML5 / CSS3(순수 CSS, 전처리기 없음) / Vanilla JS (ES5 문법, IIFE 1개)

---

## 1. 파일 구조

```
ojehouse/
├── index.html              ← 본 페이지 (전체 마크업)
├── font-preview.html       ← 폰트 컨펌용 임시 파일 (배포 전 삭제)
├── assets/
│   ├── css/style.css       ← 전체 스타일 (681줄, 단일 파일)
│   ├── js/main.js          ← 전체 스크립트 (421줄, 단일 파일)
│   └── img/                ← 이미지 16개
└── HANDOFF.md              ← 이 문서 (배포 대상 아님)
```

### 로컬 실행

`index.html` 더블클릭으로도 열리지만, 네이버 지도 API는 `file://`에서 도메인 인증이 안 되므로
지도까지 확인하려면 로컬 서버로 띄워 주세요.

```bash
cd ojehouse
python3 -m http.server 8000
# → http://localhost:8000
```

---

## 2. 설정값은 전부 `main.js` 상단에 모여 있습니다

HTML을 건드리지 않고 아래 4개 블록만 고치면 대부분의 운영 변경이 끝납니다.

| 위치 | 변수 | 역할 |
|---|---|---|
| `main.js:3` | `SHOW_JAPAN` | 일본 팝업 섹션 전체 on/off. `false`면 섹션과 관련 내비가 자동 제거됩니다. |
| `main.js:15-20` | `JAPAN_CITIES` | 도시 카드 배열. 배열에 적은 순서대로 렌더링되므로 순회 날짜순을 유지해 주세요. |
| `main.js:33-41` | `NAVER_MAP` | 지도 인증키·좌표·플레이스 링크 |
| `main.js:45-247` | `I18N` | 4개 언어(ko/en/ja/zh) 문안 사전 |
| `main.js:249` | `PRIMARY` | 언어별 대표 문의 채널 (해당 채널이 목록 맨 위로 올라가고 '추천' 배지가 붙습니다) |

### 다국어 동작 방식

- 마크업에 `data-i18n="키"`를 붙이면 언어 전환 시 `I18N[lang][키]` 값으로 `innerHTML`이 교체됩니다.
- 기본값은 HTML에 한국어로 하드코딩되어 있고, 초기화 시 `applyLang('ko')`가 한 번 실행됩니다.
- **문안을 추가할 때는 HTML의 기본 텍스트와 `I18N.ko`의 값을 함께 수정**해야 합니다. 한쪽만 고치면 언어를 한 번 전환했을 때 문구가 되돌아갑니다.
- `I18N` 값에는 `<br>`, `<strong>` 등 태그가 들어갑니다(`innerHTML`로 주입). 사용자 입력이 아닌 고정 문안이므로 현재 구조상 문제는 없지만, 추후 외부 입력이 들어갈 일이 생기면 주입 방식을 바꿔 주세요.
- 언어 상태는 저장되지 않습니다. 새로고침하면 한국어로 돌아갑니다. (브라우저 언어 자동 감지·localStorage 저장은 미구현 — 필요 시 요청 주세요.)

### 네이버 지도

`NAVER_MAP.keyId`가 비어 있으면 지도 대신 주소 안내 박스(`.map-fallback`)가 표시되도록 되어 있습니다.
현재는 **키가 비어 있는 상태**라 폴백이 보입니다.

1. 네이버 클라우드 플랫폼 > Maps > Application 등록
2. 사용 도메인(예: `https://ojehouse.com`)을 반드시 등록 — 미등록 시 인증 실패
3. 발급 키를 `keyId`에 입력
4. `keyParam`은 키 발급 시점에 따라 다릅니다. 최신 키는 `ncpKeyId`, 예전 키는 `ncpClientId`
5. `lat` / `lng`는 현재 서울시청 좌표(임시)입니다. 네이버 지도에서 실제 위치 우클릭 > '좌표 복사'로 교체
6. 인증 실패·스크립트 로딩 실패는 `window.navermap_authFailure`와 `onerror`로 모두 폴백 처리되어 있습니다

> 터치 기기에서는 드래그가 페이지 스크롤을 가로채지 않도록 지도 드래그를 잠가 두었습니다 (`main.js:332` 부근). 의도된 동작입니다.

---

## 3. 배포 전 반드시 처리해야 하는 항목

### 🔴 필수 (이 상태로 배포하면 기능이 동작하지 않습니다)

| # | 내용 | 위치 |
|---|---|---|
| 1 | **문의 채널 링크가 전부 `href="#"` 더미입니다.** 네이버플레이스 / WhatsApp / LINE / WeChat 4개의 실제 URL 연결 필요. **예약 섹션과 모달 두 군데에 동일한 목록이 있으니 8개 모두** 교체해야 합니다. | `index.html:277-280`, `index.html:350-353` |
| 2 | 네이버 지도 인증키 · 실제 좌표 입력 (위 2번 항목 참고) | `main.js:33-41` |
| 3 | 매장 주소가 `서울특별시 (상세 주소 확정 예정)` 임시값입니다. HTML 2곳 + 4개 언어 사전 모두 교체 | `index.html:300`, `index.html:307`, `main.js`의 `locAddrV` ×4 |
| 4 | 푸터 사업자등록번호가 `000-00-00000` 더미입니다. 상호·대표자명·통신판매업 신고번호 등 법적 표기 확인 필요 | `index.html:334` |
| 5 | **파비콘이 연결되어 있지 않습니다.** `assets/img/favicon-512.png` 파일은 있으나 `<link rel="icon">` 태그가 없습니다. 추가 필요 | `index.html` `<head>` |

### 🟡 권장

| # | 내용 | 비고 |
|---|---|---|
| 6 | **이미지 용량 최적화.** `oje-ceo-profil02.JPG` **16.5MB**, `oje-ceo02.JPG` 4.6MB, `oje-ceo01.JPG` 2.2MB — 원본 그대로입니다. 모바일에서 로딩이 매우 느려집니다. 리사이즈(장변 1600px 내외) + WebP 변환 권장. 나머지 이미지는 이미 최적화되어 있습니다. | 전체 49MB → 대부분이 이 3개 |
| 7 | **디스플레이 폰트 3종이 모두 로드되고 있습니다.** 고객 컨펌 후 1종만 남기고 정리해 주세요. ① `style.css:32-34`에서 확정 폰트 한 줄만 주석 해제 → ② `index.html:16` Google Fonts 링크에서 미사용 2종 제거 → ③ `font-preview.html` 삭제 | 현재 Italiana 활성 |
| 8 | 미사용 에셋 3개 삭제 가능: `logo-mark.png`, `oje-ceo-profile01.JPG`, `favicon-512.png`(5번 처리 시 사용됨) | 어디에서도 참조되지 않음 |
| 9 | OG / Twitter 카드 메타태그가 없습니다. 카톡·인스타 공유 시 썸네일이 안 나옵니다. `og:title`, `og:description`, `og:image`, `og:url` 추가 권장 | `index.html` `<head>` |
| 10 | 오타: 맨 위로 버튼 `aria-label="맨1 위로 이동"` → `"맨 위로 이동"` | `index.html:339` |
| 11 | 폰트를 Google Fonts CDN + jsDelivr(Pretendard)에서 불러옵니다. 중국 지역 접속을 고려한다면 폰트 셀프 호스팅을 검토해 주세요 | `index.html:10-17` |
| 12 | 가격이 HTML에 하드코딩되어 있습니다(모든 언어 공통 노출). 변경이 잦다면 `main.js` 설정 블록으로 빼는 것을 검토 | `index.html:130-161` |

---

## 4. 스타일 구조

- CSS 변수는 `style.css:12-34`의 `:root`에 모여 있습니다. 색상(`--ink`, `--rose` 등), 라운드(`--radius`), 최대 너비(`--maxw:1100px`), 이징(`--ease`), 폰트 스택을 여기서 일괄 관리합니다.
- 콘텐츠 최대 너비는 `.wrap` 클래스(`max-width:1100px`, 좌우 패딩 24px)로 통일되어 있습니다.
- 섹션 상하 패딩: 데스크톱 104px / 760px 이하 72px

### 반응형 브레이크포인트

컴포넌트별로 필요한 지점에서만 끊었기 때문에 값이 여러 개입니다.

| 지점 | 적용 대상 |
|---|---|
| 980px / 860px / 820px | 일본 도시 그리드, About·Artist·Service 그리드 → 1열 전환 |
| 760px | 전역 섹션 패딩 축소, 예약·지도 그리드 1열 |
| 560px | 포트폴리오·Artist 세부 조정 |
| `max-height:720px` | 낮은 뷰포트에서 히어로 높이 보정 |

검수는 **태블릿 1000px, 모바일 390px** 기준으로 진행했습니다.

---

## 5. 접근성 관련 (수정 시 유지해 주세요)

의도적으로 넣은 처리들입니다. 마크업 변경 시 함께 유지 부탁드립니다.

- `.skip-link` — 본문 바로가기 (포커스 시에만 노출)
- 언어 전환 버튼은 `aria-pressed`로 현재 언어를 표시합니다
- FAQ 아코디언은 `aria-expanded` + `aria-controls`로 연결되어 있고, 높이는 JS가 `scrollHeight`로 계산합니다. **FAQ 항목을 추가할 때 `aria-controls` / `id` 쌍(`faq-a3`…)을 맞춰 주세요.**
- 예약 모달은 포커스 트랩 + ESC 닫기 + 배경 클릭 닫기 + 열기 전 포커스 복원이 구현되어 있습니다 (`main.js:389-418`)
- `prefers-reduced-motion` 대응으로 `scroll-behavior`와 애니메이션을 차단합니다 (`style.css:39`)
- 히어로 h1은 `.visually-hidden`으로 처리되어 있습니다(로고 이미지가 시각적 제목 역할). SEO상 의도된 구조입니다.

---

## 6. 알려진 제약 / 미구현

- 온라인 예약 폼 없음 — 기획상 **채널 문의 안내만** 제공하는 구조입니다. 폼·결제 연동은 범위 밖입니다.
- 일본 팝업 일정은 정적 배열입니다. 관리자 화면은 없습니다.
- 언어 설정이 저장되지 않습니다(새로고침 시 한국어).
- 포트폴리오 이미지는 정적입니다. 갤러리·라이트박스 없음.
- 분석 도구(GA4 등) 미삽입.

---

## 7. 배포 체크리스트

- [ ] 3번 섹션의 🔴 필수 항목 5건 처리
- [ ] `font-preview.html` 삭제
- [ ] `HANDOFF.md` (이 파일) 배포본에서 제외
- [ ] 이미지 최적화 후 교체
- [ ] 네이버 클라우드 콘솔에 **운영 도메인 등록** 확인 → 지도 정상 노출
- [ ] 4개 언어 전환 시 레이아웃 깨짐 확인 (특히 영문·중문에서 텍스트 길이 변화)
- [ ] 모바일 390px에서 문의 채널 링크 실제 앱으로 연결되는지 확인
- [ ] 예약 모달 열기/닫기, FAQ 아코디언 동작 확인

---

문의사항이나 수정 요청은 전달 채널로 편하게 남겨 주세요.
